import React from 'react';
import SettingsContainer from '@/components/org-settings/SettingsContainer';

export const metadata = {
    title: 'Settings - Indigo Brokerage Admin Console',
    description: 'Manage your organization profile, subscription, and team.',
};

export default function SettingsPage() {
    return <SettingsContainer />;
}

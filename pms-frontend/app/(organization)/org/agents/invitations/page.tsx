import React from 'react';
import InviteAgentsContainer from '@/components/org-invitations/InviteAgentsContainer';

export const metadata = {
    title: 'Invite Agents - PropertyFlow Enterprise',
    description: 'Securely invite licensed agents to join your organization.',
};

export default function InviteAgentsPage() {
    return <InviteAgentsContainer />;
}
